import logging
import re
import asyncio
import json
from pathlib import Path
from typing import List
from telethon import TelegramClient, events

# --- Импортируем модуль обработки ---
from newsendingbot.offer_generator import generate_offer_async
from newsendingbot.process_logging import log_news_process
from newsendingbot.deduplicator import Deduplicator

# --- НАСТРОЙКИ ---
CONFIG_PATH = Path(__file__).resolve().parent.parent / "config" / "config.json"

def load_config():
    # Используем utf-8-sig для обработки возможного BOM
    with open(CONFIG_PATH, 'r', encoding='utf-8-sig') as f:
        return json.load(f)

cfg = load_config()
API_ID = int(cfg['api_id']) if str(cfg.get('api_id', '')).isdigit() else 0
API_HASH = cfg.get('api_hash', '*****')
BOT_TOKEN = cfg.get('bot_token', '*****')

# Глобальный семафор для ограничения одновременных запросов к LLM
llm_semaphore = asyncio.Semaphore(1)

# Множество для предотвращения повторной обработки одних и тех же ID сообщений
processed_messages = set()

# -- Дедубликатор --
deduplicator = Deduplicator(
    threshold=0.85,
    news_file="newsendingbot/queue_for_distribution_recommendations.json"
)

# --- Промты для пользователей ---
SYSTEM_PROMPT = """
Ты — аналитический агент, который помогает сотрудникам банка находить возможности для подключения новых клиентов к зарплатным проектам (ЗП-проектам).

Тебе на вход подаётся текст одной новости.
Твоя задача — проанализировать содержание этой новости и определить, даёт ли она потенциальную возможность предложить зарплатный проект.

🔸 Как действовать:

ПЕРВОЕ И ГЛАВНОЕ ПРАВИЛО:
Проверь, есть ли в тексте новости КОНКРЕТНОЕ НАЗВАНИЕ компании (например, "ООО Ромашка", "завод 'Звезда'", "компания 'СтройМаш'") или ИМЯ предпринимателя (например, "ИП Иванов", "бизнесмен Петр Петров").
Если названия или имени НЕТ (например, просто написано "открылся новый завод", "инвестор построит цех", "компания получила субсидию" без указания названия) — СРАЗУ отвечай:
«Нет предложений»
Не придумывай рекомендации, если нет конкретного клиента.

ВТОРОЕ ПРАВИЛО (ЛИМИТЫ):
Даже если есть название клиента, проверь масштаб события.
Если сумма инвестиций/субсидий/гранта МЕНЬШЕ 10 млн рублей — СРАЗУ отвечай «Нет предложений».
Если создаётся 3 или МЕНЬШЕ рабочих мест — СРАЗУ отвечай «Нет предложений».
Если точных цифр нет, но по контексту понятно, что масштаб мелкий (например, "грант на швейную машинку", "открыл киоск", "купил станок") — СРАЗУ отвечай «Нет предложений».

Исключение: Если цифр нет, но очевидно, что объект крупный (Торговый Центр, Завод, Фабрика, Жилой Комплекс, и тд.) — тогда давай рекомендацию.

ТРЕТЬЕ ПРАВИЛО (СМЕНА РУКОВОДСТВА):
Если в новости говорится о смене генерального директора, управляющего или руководителя в КРУПНОЙ компании или на заводе (даже если нет инфо о новых рабочих местах):
Твоя рекомендация должна быть строго такой:
«Встретиться с новым руководителем для обсуждения сотрудничества»

ЧЕТВЕРТОЕ ПРАВИЛО:
Игнорируй слишком общие новости (встречи правительства, обсуждение мер поддержки, статистика), если там нет конкретных компаний.

Только если есть название клиента, проверяй суть события:
Читай текст новости и выделяй суть события.
Если в новости описано событие, которое подразумевает:
появление новых рабочих мест (например, открытие завода, фабрики, офиса, филиала, найм персонала);
расширение или запуск бизнеса (например, компания получила субсидию, открыла новое производство, модернизирует оборудование и, вероятно, нанимает сотрудников);
программы поддержки малого/среднего бизнеса, стартапов, гранты на развитие предприятий;
любые меры, которые могут привести к росту числа сотрудников или появлению новых организаций, которым нужно будет выплачивать зарплаты;

И при этом если в новости сказано, что действие происходит в данном списке регионов, либо регион определить невозможно:
1. Хабаровский край
2. Приморский край
3. Сахалинская область
4. Еврейская автономная область  
5. Амурская область    
6. Чукотский автономный округ
7. Камчатский край  
8. Магаданская область

— напиши короткую рекомендацию, пояснив, что именно в новости является точкой входа для предложения ЗП-проекта.

Если регион определить невозможно, то упоминай это в своём ответе в таком виде:
Текст рекомендации и тд. В новости не указан регион, необходимо проверить

Если можно понять, что это за регион, но он НЕ входит в список выше, то твой ответ должен быть:
«Нет предложений»



🔸 Формат ответа:

Пиши только краткую рекомендацию или точную фразу «Нет предложений».
Не пересказывай всю новость и не добавляй лишних комментариев, приветствий.
Ответ должен быть чётким и понятным сотруднику банка.

Перед твоим ответом вставляй ссылку на источник в формате
Ссылка на источник: ссылка
Потом очень кратко, о чём новость, буквально несколько слов
А перед самой рекомендацией вставляй <b>Рекомендация:</b>

Таким образом финальная структура твоего ответа следующая:

Ссылка на источник: ссылка
Очень кратко о чём новость\n
<b>Рекомендация:</b>\n
Описание предложения

Если в новости нет ничего, что связано с появлением новых рабочих мест, расширением бизнеса или потенциальными клиентами для ЗП-проекта (например, общие прогнозы по экономике, повышение налогов, культурные мероприятия и т. п.)
или
Если по новости понятно, что потенциал зарплатных проектов менее трёх человек
или
Если в новости предоставляются льготы или бонусы для САМОЗАНЯТЫХ
или
Если в новости речь идёт о бонусах/льготах/субсидиях для сельского хозяйства (в случае сельского хозяства интересует только открытие нового предприятия)
или
сказано, что дейсвтие происходит не в регионе из указнного выше списка, ответь строго фразой:
Ссылка на источник: ссылка
«Нет предложений»


Примеры работы:

Пример 1 (ЕСТЬ название, КРУПНЫЙ проект):
Ссылка на источник: ссылка 1
Компания "Айсберг" запускает новую линию производства в Хабаровске, инвестиции 50 млн руб.\n 
<b>Рекомендация:</b> 
Предложить компании "Айсберг" подключить зарплатный проект для новых сотрудников.

Пример 2 (НЕТ названия - ИГНОРИРОВАТЬ):
Ссылка на источник: ссылка 2
В Приморье открылся новый рыбоперерабатывающий завод, создано 50 рабочих мест.\n 
<b>Рекомендация:</b> 
Нет предложений

Пример 3 (ЕСТЬ название, но МАЛО рабочих мест - ИГНОРИРОВАТЬ):
Ссылка на источник: ссылка 3
ИП Петров открыл точку кофе с собой, нанял 2 бариста.\n
<b>Рекомендация:</b>
Нет предложений

Пример 4 (ЕСТЬ название, но МАЛЕНЬКАЯ сумма - ИГНОРИРОВАТЬ):
Ссылка на источник: ссылка 4
Фермер Иванов получил грант 3 млн рублей на покупку трактора.\n
<b>Рекомендация:</b>
Нет предложений

Пример 5 (ЕСТЬ название, нет цифр, но КРУПНЫЙ объект):
Ссылка на источник: ссылка 5
Завод "МеталлСтрой" начал строительство нового цеха.\n 
<b>Рекомендация:</b> 
Предложить заводу "МеталлСтрой" подключить зарплатный проект. В новости не указан регион, необходимо проверить
    """

# Каналы, которые отслеживаем
CHANNELS = {
    '@businessnews27',
    '@RGSNIK27',
}

# --- Маппинг регион -> список user_id ---
REGION_USERS = {
    "Хабаровский край": [], # Добавьте ID пользователей
    "Приморский край": [],
    "Сахалинская область": [],
    "Еврейская автономная область": [],
    "Амурская область": [],
    "Чукотский автономный округ": [],
    "Камчатский край": [],
    "Магаданская область": [],
}

# --- Список "других" регионов ---
OTHER_REGIONS_LIST = [
    "Республика Адыгея", "Майкоп",
    "Республика Башкортостан", "Уфа",
    "Республика Бурятия", "Улан-Удэ",
    "Республика Алтай", "Горно-Алтайск",
    "Республика Дагестан", "Махачкала",
    "Республика Ингушетия", "Магас",
    "Кабардино-Балкарская Республика", "Нальчик",
    "Республика Калмыкия", "Элиста",
    "Республика Карачаево-Черкесия", "Черкесск",
    "Республика Карелия", "Петрозаводск",
    "Республика Коми", "Сыктывкар",
    "Республика Марий Эл", "Йошкар-Ола",
    "Республика Мордовия", "Саранск",
    "Республика Саха (Якутия)", "Якутск",
    "Республика Северная Осетия-Алания", "Владикавказ",
    "Республика Татарстан", "Казань",
    "Республика Тыва", "Кызыл",
    "Удмуртская Республика", "Ижевск",
    "Республика Хакасия", "Абакан",
    "Чеченская Республика", "Грозный",
    "Чувашская Республика", "Чебоксары",
    "Алтайский край", "Барнаул",
    "Краснодарский край", "Краснодар",
    "Красноярский край", "Красноярск",
    "Ставропольский край", "Ставрополь",
    "Архангельская область", "Архангельск",
    "Астраханская область", "Астрахань",
    "Белгородская область", "Белгород",
    "Брянская область", "Брянск",
    "Владимирская область", "Владимир",
    "Волгоградская область", "Волгоград",
    "Вологодская область", "Вологда",
    "Воронежская область", "Воронеж",
    "Ивановская область", "Иваново",
    "Иркутская область", "Иркутск",
    "Калининградская область", "Калининград",
    "Калужская область", "Калуга",
    "Кемеровская область", "Кемерово",
    "Кировская область", "Киров",
    "Костромская область", "Кострома",
    "Курганская область", "Курган",
    "Курская область", "Курск",
    "Ленинградская область", "Санкт-Петербург",
    "Липецкая область", "Липецк",
    "Московская область", "Москва",
    "Мурманская область", "Мурманск",
    "Нижегородская область", "Нижний Новгород",
    "Новгородская область", "Великий Новгород",
    "Новосибирская область", "Новосибирск",
    "Омская область", "Омск",
    "Оренбургская область", "Оренбург",
    "Орловская область", "Орел",
    "Пензенская область", "Пенза",
    "Пермский край", "Пермь",
    "Псковская область", "Псков",
    "Ростовская область", "Ростов-на-Дону",
    "Рязанская область", "Рязань",
    "Самарская область", "Самара",
    "Саратовская область", "Саратов",
    "Свердловская область", "Екатеринбург",
    "Смоленская область", "Смоленск",
    "Тамбовская область", "Тамбов",
    "Тверская область", "Тверь",
    "Томская область", "Томск",
    "Тульская область", "Тула",
    "Тюменская область", "Тюмень",
    "Ульяновская область", "Ульяновск",
    "Челябинская область", "Челябинск",
    "Забайкальский край", "Чита",
    "Ярославская область", "Ярославль",
    "г. Москва", "г. Санкт-Петербург",
    "Ненецкий автономный округ", "Нарьян-Мар",
    "Ханты-Мансийский автономный округ - Югра", "Ханты-Мансийск",
    "Ямало-Ненецкий автономный округ", "Салехард"
]

def build_flexible_other_regions_pattern(regions_list):
    roots = set()
    for name in regions_list:
        n = name.lower().strip()
        n = re.sub(r"\b(респ\.?|республика|обл\.?|область|край|ао|автономный|г\.?|город)\b", "", n)
        n = re.sub(r"[^а-яё\- ]", " ", n)
        n = n.strip()
        if not n:
            continue
        for part in n.split():
            if len(part) > 4:
                roots.add(re.escape(part[:6]))
            else:
                roots.add(re.escape(part))
    alternation = "|".join(sorted(roots, key=lambda x: -len(x)))
    pattern = rf"\b(?:{alternation})[а-яё\-]{{0,5}}\b"
    return re.compile(pattern, flags=re.IGNORECASE)

OTHER_REGIONS_PATTERN = build_flexible_other_regions_pattern(OTHER_REGIONS_LIST)

def detect_other_regions(text: str):
    if not text:
        return []
    found = set()
    for m in OTHER_REGIONS_PATTERN.finditer(text):
        found.add(m.group(0))
    return list(found)

def detect_regions(text: str) -> List[str]:
    if not text:
        return []
    regions_found = set()
    t = text.lower()
    patterns = {
        "Хабаровский край": r"\b(?:хабаровск(?:\w{0,7})|хабаровск(?:\w{0,7})?\s*кра(?:\w{0,4})|комсомольск(?:\w{0,7})?[-\s]?на[-\s]?амуре|николаевск(?:\w{0,7})?[-\s]?на[-\s]?амуре|советск(?:\w{0,7})\sгаван(?:\w{0,7}))\b",
        "Приморский край": r"\b(?:приморск(?:ий|\w{0,7})?|приморь(?:е|\w{0,7})?|владивосток(?:\w{0,7})|уссурийск(?:\w{0,7})|находк(?:\w{0,7})?|арсеньев(?:\w{0,7})|артем(?:\w{0,7})|лесозаводск(?:\w{0,7}))\b",
        "Сахалинская область": r"\b(?:сахалин(?:\w{0,7})|южно[-\s]?сахалинск(?:\w{0,7})|корсаков(?:\w{0,7})|холмск(?:\w{0,7})|остров(?:а\s+курильск(?:ие|их))?)\b",
        "Еврейская автономная область": r"\b(?:евре(?:йск(?:\w{0,7})?\s+автономн(?:\w{0,7})?(?:\s+область|)|еврейская\s+ао)|биробиджан(?:\w{0,7})|облучье(?:\w{0,7})|EАО)\b",
        "Амурская область": r"\b(?:амурск(?:ая|ой|ую|ом|\w{0,5})?|благовещенск(?:\w{0,7})|свободный(?:\w{0,7})|тында(?:\w{0,7})|зея(?:\w{0,7})|шимановск(?:\w{0,7}))\b",
        "Чукотский автономный округ": r"\b(?:чукотск(?:\w{0,7})?|чукотка(?:\w{0,7})|анадыр(?:ь|е|я)?|певек(?:\w{0,7})|беринговск(?:\w{0,7}))\b",
        "Камчатский край": r"\b(?:камчатск(?:\w{0,7})?|петропавловск[-\s]?камчатск(?:\w{0,7})?|елизово(?:\w{0,7})|магаданск(?:\w{0,7})?)\b",
        "Магаданская область": r"\b(?:магадан(?:\w{0,7})|магаданск(?:\w{0,7})?|сеяха(?:\w{0,7}))\b"
    }
    for region, pat in patterns.items():
        try:
            regex = re.compile(pat, flags=re.IGNORECASE | re.VERBOSE)
            if regex.search(t):
                regions_found.add(region)
        except re.error:
            if region.lower().split()[0] in t:
                regions_found.add(region)
    return list(regions_found)

# Логирование
logging.basicConfig(
    format="%(asctime)s - %(name)s - %(levelname)s - %(message)s",
    level=logging.INFO
)
logger = logging.getLogger(__name__)

# Инициализация клиента Telethon
SESSION_NAME = str(Path(__file__).resolve().parent / "newsendingbot_session")
client = TelegramClient(SESSION_NAME, API_ID, API_HASH)

# Множество для хранения ID обработанных сообщений
processed_messages = set()

@client.on(events.NewMessage)
async def handle_new_message(event):
    try:
        if not event.is_channel:
            return
            
        sender = await event.get_chat()
        channel_username = getattr(sender, 'username', None)
        channel_key = f"@{channel_username}" if channel_username else None
        
        if channel_key and channel_key not in CHANNELS:
            return

        message = event.message
        if not (message.text or message.caption):
            return

        # 1. Проверка по ID сообщения (предотвращаем двойную обработку одного и того же события в коде)
        msg_internal_id = f"{channel_key}_{message.id}"
        if msg_internal_id in processed_messages:
            return
        processed_messages.add(msg_internal_id)
        if len(processed_messages) > 1000:
            list_ids = list(processed_messages)
            processed_messages.clear()
            processed_messages.update(list_ids[-500:])

        original_text = message.text or message.caption

        # --- Разбиваем пачку на отдельные новости ---
        news_blocks = [
            block.strip()
            for block in original_text.split("──────────")
            if block.strip()
        ]  

        for news in news_blocks:
            regions = detect_regions(news) 
            other_regions = detect_other_regions(news)

            if not regions and other_regions:
                logger.info("Новость содержит прочие регионы — пропускаем.")
                continue

            # Определяем список всех потенциальных получателей
            if regions:
                target_user_ids = set()
                for r in regions:
                    ulist = REGION_USERS.get(r, [])
                    for uid in ulist:
                        target_user_ids.add(uid)
            else:
                target_user_ids = set(uid for ulist in REGION_USERS.values() for uid in ulist)

            if not target_user_ids:
                continue

            # Фильтруем пользователей, которые ЕЩЕ НЕ видели эту новость (базовая дедупликация)
            actual_recipients = []
            for uid in target_user_ids:
                if not deduplicator.is_duplicate(news, user_id=uid):
                    actual_recipients.append(uid)

            if not actual_recipients:
                continue

            # Генерируем оффер ОДИН РАЗ на всю группу пользователей
            async with llm_semaphore:
                offer = await generate_offer_async(news, SYSTEM_PROMPT)
                await asyncio.sleep(1.0)
            
            if not offer or "Нет предложений" in offer:
                continue

            # Рассылаем один и тот же оффер всем подходящим пользователям
            region_str = ", ".join(regions) if regions else "Не определен"
            
            for uid in actual_recipients:
                try:
                    await client.send_message(uid, offer, parse_mode='html')
                    deduplicator.add(news, user_id=uid)
                    logger.info(f"Оффер отправлен пользователю {uid}")
                except Exception as e:
                    logger.error(f"Ошибка отправки пользователю {uid}: {e}")

            # Логируем процесс один раз для всей группы
            try:
                log_news_process(news, offer, region_str, actual_recipients)
            except Exception as e:
                logger.error(f"Ошибка логирования: {e}")

    except Exception as e:
        logger.exception("Ошибка при обработке сообщения")

async def main():
    logger.info("News Sending Bot (Telethon) запускается...")
    await client.start(bot_token=BOT_TOKEN)
    logger.info("Бот на связи!")
    await client.run_until_disconnected()

if __name__ == "__main__":
    asyncio.run(main())
